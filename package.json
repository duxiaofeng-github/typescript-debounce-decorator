{
  "name": "typescript-debounce-decorator",
  "version": "0.0.1",
  "description": "a debounce decorator for typescript function",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/tsc && ./node_modules/.bin/ava --verbose && rm -f test.js src/index.js",
    "build": "rm -f index.js && ./node_modules/.bin/rollup -c"
  },
  "keywords": [
    "typescript",
    "debounce",
    "decorator",
    "typescript-debounce-decorator"
  ],
  "author": "duxiaofeng-github",
  "license": "MIT",
  "typed": "index.d.ts",
  "devDependencies": {
    "ava": "^0.24.0",
    "rollup": "^0.53.3",
    "rollup-plugin-typescript2": "^0.9.0",
    "rollup-plugin-uglify": "^2.0.1",
    "typescript": "^2.6.2"
  }
}
